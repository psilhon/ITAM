# ITAM 版本管理

## 概述

本项目采用语义化版本号（SemVer）:
- **主版本号**: 不兼容的重大架构变更
- **次版本号**: 向下兼容的功能新增
- **修订号**: 向下兼容的问题修复

格式: `v主版本.次版本.修订号` (例: v4.5.0)

---

## 版本历史

### v4.5.4 (2026-04-20)

**类型**: Bug 修复 + 防护增强

**本次更新内容**:

#### 1. 关键 Bug 修复 — 编辑服务器导致数据丢失
- 🐛 **问题**：编辑服务器时，未填写的可选字段会被空字符串覆盖，导致已有数据被清空
- 🛡️ **根因分析**：前端全量提交 → Controller null→"" 转换 → Prisma 全量覆盖
- ✅ **修复**：双层防护机制
  - **第 1 层（前端源头治理）**：编辑模式下只提交变更过的字段（diff 提交）
  - **第 2 层（Service 兜底）**：Service 层过滤 26 个可选字段的空字符串写入

#### 2. 前端优化
- 🛠️ 编辑表单增加 `originalForm` 状态记录，提交时逐字段对比只发变更
- 🐛 修复 `rackUnit` 字段在新建提交时被遗漏的 bug
- ✅ `name` 和 `status` 必填字段始终发送，确保不会缺失

#### 3. 版本管理
- ✅ 项目接入 GitHub（https://github.com/psilhon/ITAM）
- ✅ 完善 `.gitignore`（排除 .workbuddy/、backups/）
- ✅ 添加 README.md 项目说明文档

**改动文件**:
- `frontend/src/components/ServerFormDrawer.tsx` — diff 提交逻辑
- `backend/src/services/server.service.ts` — 空字符串过滤
- `README.md` — 项目说明文档

---

### v4.5.3 (2026-04-10)

**类型**: 功能增强

**本次更新内容**:

#### 1. 后端功能增强
- ✅ **批量导入接口**: 新增 `/api/servers/batch-import` 接口
- ✅ **不区分大小写匹配**: 服务器和网络设备导入均支持
- ✅ **智能合并**: 重复资产自动更新空字段
- ✅ **详细报告**: 提供文件级和汇总统计报告

#### 2. 前端界面升级
- ✅ **导入模式切换**: 支持单文件和批量导入模式
- ✅ **多选文件支持**: 批量模式下可同时选择多个 `.txt` 文件
- ✅ **增强结果展示**:
  - 汇总统计面板（文件数、成功数、服务器数等）
  - 每个文件的详细导入结果
  - 友好的错误信息展示
- ✅ **用户指导**: 添加多选文件操作提示
- ✅ **状态管理**: 完整的导入状态和进度显示

#### 3. 技术优化
- ✅ **前端**: 重写 `handleFileChange` 函数，支持两种导入模式
- ✅ **API**: 新增 `batchImportServers` 函数，完善类型定义
- ✅ **查询优化**: 服务器导入使用 `in` 查询替代 `contains`
- ✅ **性能**: 并行文件读取和处理

#### 4. 新增文档
- ✅ `资产批量导入功能说明.md` - 后端功能文档
- ✅ `前端批量导入使用指南.md` - 用户操作指南
- ✅ `批量导入测试样例文件.md` - 测试示例和规范

**技术细节**:
- **后端核心**:
  - `advanced-import.service.ts` - 多文件批量导入服务
  - 服务器导入：`name: { in: namesLower }` 精确查询
  - 网络设备导入：手动小写匹配，避免误匹配
- **前端核心**:
  - `ServerList.tsx` - 重写导入界面，支持模式切换
  - `multiple={importMode === 'batch'}` - 动态控制多选
  - 详细的批量导入结果展示组件

**验证结果**:
- ✅ API 健康检查通过
- ✅ 批量导入接口测试成功
- ✅ 前端构建和部署成功
- ✅ 多选文件功能正常工作
- ✅ 模式切换和结果展示正常
- ✅ 服务运行稳定，无错误日志

**用户界面截图**:
```
┌────────────────────────────────────────────┐
│             导入资产                        │
├────────────────────────────────────────────┤
│ [单文件导入]  [批量导入（多选文件）]         │
│                                            │
│ 请选择多个 SeverCollect.txt 格式的文件：   │
│ [选择文件]                                 │
│ (按住 Ctrl/Command 键可多选文件)           │
└────────────────────────────────────────────┘
```

**使用场景**:
1. **批量资产导入**: 同时导入多个机房的资产数据
2. **季度盘点**: 导入当季所有服务器的更新信息
3. **新机房上线**: 一次性导入新机房的所有设备
4. **数据迁移**: 从旧系统迁移大量资产数据

---

### v4.5.2 (2026-04-09)

**类型**: 功能更新

**本次更新内容**:
1. 资产导入功能增强
   - 服务器导入：资产名称匹配时不区分大小写
   - 网络设备导入：资产名称匹配时不区分大小写
   - 例：`Server-01` 和 `server-01` 视为同一资产

**技术细节**:
- 服务器导入：`import.service.ts` - 使用 `name.toLowerCase()` 匹配
- 网络设备导入：`network-device-import.service.ts` - 使用 `contains` + 精确匹配

**验证结果**:
- ✅ API 健康检查通过
- ✅ 版本接口正常 (v4.5.2)
- ✅ 服务运行正常

---

### v4.5.1 (2026-04-08)

**类型**: 修订版更新

**本次更新内容**:
1. 版本管理体系完善
   - 取消 PM2 开机自启动
   - 增强备份功能（完整备份程序+数据）
   - 更新脚本增加 4 项强制验证

2. 备份功能增强
   - 备份内容：数据库 + 后端程序 + 前端程序 + 配置文件 + 脚本
   - 自动验证备份有效性
   - 备份记录包含数据统计

3. 更新流程完善
   - 更新前记录版本状态
   - 更新后强制验证 4 项核心功能
   - 验证失败自动提示回滚方案

**备份验证结果**:
- ✅ 数据库: 83 条服务器记录, 4 条网络设备
- ✅ 后端构建: 正常
- ✅ 前端构建: 正常
- ✅ 配置文件: 完整

**更新验证结果**:
- ✅ API 健康检查通过
- ✅ 首页访问正常 (HTTP 200)
- ✅ 版本接口正常
- ✅ 服务器 API 正常

---

### v4.5.0 (2026-04-08)

**类型**: 次版本更新

**本次更新内容**:
1. 部署方式优化
   - Docker 部署 → PM2 本地部署
   - 新增 `start-local.sh` 启动脚本
   - 新增 `ecosystem.config.js` PM2 配置

2. 项目精简
   - 移除 Docker 相关文件
   - 清理冗余代码和空目录
   - 优化项目结构

3. 文档整理
   - 创建 `docs/系统说明文档.md`
   - 创建 `docs/系统使用文档.md`

4. 问题修复
   - 修复服务器列表表格悬停抖动问题
   - 修复布局问题（网络设备页面空白）
   - 修复 `backdrop-filter: blur()` 导致的渲染问题

**系统状态**:
- 访问地址: http://localhost:3001
- 数据库: SQLite (backend/prisma/dev.db)
- 进程管理: PM2

**配置文件**:
- `ecosystem.config.js` - PM2 配置
- `start-local.sh` - 启动脚本
- `backup.sh` - 备份脚本
- `update.sh` - 更新脚本

---

## 版本更新流程

每次更新系统前，必须：

1. **备份当前系统**
   ```bash
   ./backup.sh
   ```

2. **执行更新**
   ```bash
   ./update.sh
   ```

3. **功能验证**
   ```bash
   curl http://localhost:3001/api/health
   ```

4. **更新文档**
   - 在 CHANGELOG.md 追加新版本记录
   - 记录更新内容、文件变更、注意事项

---

## 备份说明

备份文件保存在 `backups/` 目录，按日期组织：

```
backups/
├── 2026-04-08/
│   ├── database/        # 数据库备份
│   ├── config/         # 配置文件备份
│   └── changelog.md     # 备份时的版本记录
```

备份内容包括：
- 数据库文件 (dev.db)
- PM2 配置
- 环境变量
- 启动脚本

---

## 回滚流程

如需回滚到上一版本：

1. 停止当前服务
   ```bash
   ./start-local.sh stop
   ```

2. 恢复数据库
   ```bash
   cp backups/YYYY-MM-DD/database/dev.db backend/prisma/dev.db
   ```

3. 恢复配置文件
   ```bash
   cp backups/YYYY-MM-DD/config/* .
   ```

4. 重启服务
   ```bash
   ./start-local.sh start
   ```
